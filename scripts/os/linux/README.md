# Linux Installer Scripts

This directory contains Linux-specific bootstrap entrypoints.

## Files

- `install/ensure-codex.sh`: installs or verifies the required Codex CLI version on Linux
- `install/ensure-claude-code.sh`: optional Claude Code bootstrap for Linux

Linux is the current primary exported runtime payload, so this installer path matches the current native export profile.
