interface:
  display_name: "Manual Tester"
  short_description: "Realistic flow testing with evidence"
  default_prompt: "Use $manual-tester to validate this user flow end-to-end and return pass/fail results with reproducible evidence."

dependencies:
  tools:
    - type: "mcp"
      value: "chrome-devtools"
      description: "Browser automation and inspection for realistic flows"

policy:
  allow_implicit_invocation: true
